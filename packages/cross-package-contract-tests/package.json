{
  "name": "@fffattiger/pix-cross-package-contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "TEST-ONLY cross-package semantic/architecture contract tests: parity between otherwise-independent package projections. No production exports, no runtime consumers — the sole purpose is pinning frozen projections (Protocol DTOs, Host sanitization) to their canonical runtime-core authority across package boundaries without weakening architecture gates.",
  "type": "module",
  "scripts": {
    "build:deps": "npm run build --workspace @fffattiger/pix-runtime-core && npm run build --workspace @fffattiger/pix-protocol",
    "prebuild": "npm run build:deps",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build:deps",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build:deps",
    "test": "tsc -p tsconfig.json && node ../../scripts/run-node-test.mjs \"dist-test/**/*.test.js\"",
    "clean": "node ../../scripts/remove-paths.mjs dist-test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@fffattiger/pix-runtime-core": "file:../runtime-core",
    "@fffattiger/pix-protocol": "file:../protocol"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
