{
  "name": "@fffattiger/pix-runtime-contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "TEST-ONLY reusable adapter contract suite and reference fake; excluded from production package allowlists",
  "keywords": ["test-only", "adapter-contract"],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./suite": {
      "types": "./dist/suite.d.ts",
      "import": "./dist/suite.js"
    },
    "./reference": {
      "types": "./dist/reference.d.ts",
      "import": "./dist/reference.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:core": "npm run build --workspace @fffattiger/pix-runtime-core",
    "build:deps": "npm run build --workspace @fffattiger/pix-runtime-core && npm run build --workspace @fffattiger/pix-protocol",
    "prebuild": "npm run build:deps",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run build:deps",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prebuild:test": "npm run build:deps",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node ../../scripts/run-node-test.mjs \"dist-test/**/*.test.js\"",
    "clean": "node ../../scripts/remove-paths.mjs dist dist-test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@fffattiger/pix-runtime-core": "file:../runtime-core",
    "@fffattiger/pix-protocol": "file:../protocol"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
