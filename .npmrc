# Building pix from source requires workspace devDependencies even when the
# surrounding service exports NODE_ENV=production. Runtime/package-only install
# behavior will be defined separately by REL1 packaging.
include=dev
